export enum Route {
  GTC = '/gtc',
  HOME = '/',
  IMPRINT = '/imprint',
  LOGIN = '/login',
  NOT_FOUND = '/not-found',
  PASSWORD_RESET = '/password-reset',
  SIGN_UP = '/sign-up',
  VERIFY_EMAIL = '/verify-email',
}
